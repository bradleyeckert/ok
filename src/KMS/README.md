# kms

`int KMSlookup(uint8_t *DeviceName, uint8_t **key)` looks up and outputs the device key based on the device's mole port boilerplate (DeviceName).
It returns 0 for success.

`kms.c` is the trivial version, used for testing.
